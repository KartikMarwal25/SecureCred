# Base URL of the SecureCred API (see apps/api). Includes the version prefix.
VITE_API_BASE_URL=http://localhost:4000/api/v1

# Clerk publishable key. Leave empty to run the app in local dev-auth mode
# (see src/auth/devAuth.js) with no external Clerk account required.
VITE_CLERK_PUBLISHABLE_KEY=
